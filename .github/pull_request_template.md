## 목적

<!--
무엇이 문제였나. "무엇을 했다"가 아니라 "무엇이 없거나 틀렸다"를 쓴다.
이 PR이 없으면 무엇이 막히는지가 드러나야 한다.
예: 정책 추론이 제어 루프와 같은 스레드에서 돌아 추론이 늦으면 deadline을 놓친다.
-->

Fixes TY-

## 구현

<!--
무엇을 어떻게 했나. 파일 나열이 아니라 동작·구조 단위로 쓴다 (diff는 리뷰어가 본다).
읽는 사람이 코드를 열지 않고도 뼈대를 그릴 수 있을 정도로.
예: - action chunk 링버퍼를 두고 제어 루프는 버퍼에서만 읽는다. 추론 노드는 뒤에서 채운다.
-->

-
-

## 판단

<!--
갈렸던 선택과 근거. 버린 대안이 있으면 왜 버렸는지.
6개월 뒤 "왜 이렇게 했지"를 묻게 될 지점만 남긴다. 없으면 "해당 없음".
예: MoveIt 대신 kdl-parser. v0에 필요한 건 6-DoF IK 하나뿐이고 MoveIt은 의존성이 너무 크다.
-->

해당 없음

## 완료 조건

<!-- 티켓의 완료 조건을 옮겨 적고 체크. 충족하지 못한 항목은 지우지 말고 이유를 남긴다. -->

- [ ]
- [ ]

## 수치 / 근거

<!--
측정이 필요한 티켓은 숫자를 남긴다. 해당 없으면 "해당 없음".
예: 제어 루프 지터 p99 1.4ms · 카메라 30.0Hz 유지
예: 성공률 7/10 (Wilson 95% CI 0.40–0.89) · 추론 레이턴시 p50 22ms / p99 41ms
-->

해당 없음

## 확인 방법

<!-- 리뷰어(=미래의 나)가 이 브랜치에서 그대로 실행할 수 있는 명령. -->

```bash
```

## 남은 것

<!--
이 PR에서 일부러 하지 않은 것과 후속 티켓. 알려진 한계.
"나중에 하자"를 여기 적어두지 않으면 사라진다. 없으면 "해당 없음".
-->

해당 없음

## 체크리스트

- [ ] CI 통과
- [ ] 인터페이스 계약(`docs/interfaces.md`)을 바꿨다면 문서도 같이 수정
- [ ] 평가 수치를 바꾸는 변경이면 같은 조건표로 재측정
